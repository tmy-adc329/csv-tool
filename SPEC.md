# 仕様書：店舗別売上集計ツール

## 想定発注者

**小規模カフェチェーンの本部担当者（30代・非エンジニア）**

実在の類似案件（ランサーズ「CSV 集計」466件より）:
- 在庫管理システム（小売、20〜30万円）: CSVの商品・売上データを取り込んで一元管理
- 勤怠タイムカードのマクロ（人材派遣、6〜7万円）: CSVを2種のExcel形式に変換
- 受発注照合VBA（商社、10〜20万円）: 新規受注と在庫をCSVで突合

### 発注者の状況（提案文にそのまま書ける形で）

> 5店舗を運営していて、POSレジから店舗ごと・月ごとに売上CSVが出ます。
> それを毎月手でExcelに開いてコピペし、店舗別・カテゴリ別の集計表を作って
> 月次会議の資料にしています。3ヶ月分で15ファイル。**毎月3時間くらいかかります。**
> 店舗によってCSVの列名が微妙に違ったり、金額にカンマが入っていたりして、
> 貼り付けたあとに手で直す作業が地味に一番しんどいです。

### 発注者が欲しい結果

フォルダにCSVを入れて実行するだけで、会議にそのまま出せるExcelが1つできること。
**Pythonが何かは知らないし、知りたくもない。**

## 機能要件

### 入力
- `data/` 配下の `sales_YYYYMM_<店舗slug>.csv` を全て読む
- ファイル名から「年月」と「店舗」を取得する
- 列名の揺れを吸収する（`売上金額` / `金額` → 内部的には同じもの）
- 日付形式の揺れを吸収する（`2026-06-01` / `2026/7/1`）
- カンマ付き金額を数値に変換する（`"2,040"` → `2040`）
- 空行を無視する

### 集計
1. **店舗別 × 月別**の売上合計
2. **カテゴリ別 × 月別**の売上合計
3. **商品別**の売上合計（売上金額の降順、上位10件）
4. 全体サマリ（総売上、総点数、平均単価）

### 出力
- `output/売上集計_YYYYMM.xlsx` を1ファイル出力
- シートを4枚に分ける（店舗別 / カテゴリ別 / 商品別 / サマリ）
- 金額は3桁区切り、見出しは太字、列幅を内容に合わせる
- **Excelを開いてそのまま会議資料になる状態**にする

### 実行方法
```
python main.py
```
引数なしで動くこと。発注者にコマンドライン引数を覚えさせない。

## 非機能要件

- 入力ファイルが1つ壊れていても、**どのファイルの何行目が原因か**を表示して止まる
  （「エラーが出ました」だけで終わらせない ← ここが素人との差になる）
- 処理時間は数秒以内
- 依存ライブラリは `requirements.txt` に書く

## スコープ外（今回はやらない）

- GUI
- データベース
- 前月比・前年比などの分析
- グラフの自動生成

（※実案件ではここが追加要望として来る。今回は「型」を完成させることを優先）

## 完成の判定基準

1. `python main.py` が通り、Excelが4シートで出る
2. **コードの任意の行を指されて、何をしているか説明できる**
3. 入力ファイルを1つ壊しても、原因を指す形で落ちる
4. READMEに「Before: 3時間 → After: 数秒」が書いてある
5. 30秒のデモGIFがREADMEの先頭にある

## 進め方（Phase 1 の制約）

1. まず自分で設計する。どのファイルに何を書くか、日本語でこのSPECに追記する
2. 自分で書く。詰まったらClaudeに「なぜ動かないか」を聞く（修正コードはもらわない）
3. 動いたらClaudeにレビューさせる（「問題点を指摘して。直さないで」）
4. 各行を声に出して説明できるか自己テストする

## 設計メモ

### ファイル構成と、なぜ分けるか

- config.py: 決めごと（定数）だけを置く。店舗slug→日本語店名のマップ、列名の別名リスト（売上金額／金額）、商品別ランキング件数（10）、出力ファイル名の型。
  ／なぜ分けるか：これらは「処理」ではなく「知識」。店舗や列名が増えたとき、コード中の if を探し回らず、この1ファイルに1行足すだけで済ませたい。テストからも同じ定数を参照できる。

- loader.py: data/ の15ファイルを読み、1行1明細・固定スキーマの「きれいな縦持ち表」1枚にする。担当＝ファイル探索(glob)、ファイル名パース（年月・店舗）、BOM付きUTF-8の読み込み、列名の統一、日付の変換、カンマ付き金額の数値化、空行の除去、「ファイル名」「元の行番号」列の付与。中は小さな関数（parse_amount／日付変換／列名統一／parse_filename／1ファイル読み込み／全体連結）に割る。
  ／なぜ分けるか：このツールの価値と難所は「汚い入力の正規化」に集中している。1つの巨大関数にすると、エラーが起きた行を特定できない。小関数に割ると単体で確認でき、「何行目で失敗」を出せる。

- aggregate.py: 「きれいな1枚の表」を受け取り、4種類の集計表を返す（店舗別×月、カテゴリ別×月、商品別トップ10、全体サマリ）。ファイルもExcelも知らない純粋な計算だけ。
  ／なぜ分けるか：ここが数字の心臓部で一番テストしたい。純粋関数なら本物の15ファイルなしで、手書きの小さな表で「合計が合うか」を確認できる。集計軸が増えても（例：曜日別）ここだけ足せばよい。

- excel.py: 4つの集計表と対象年月を受け取り、output/売上集計_YYYYMM.xlsx を1つ書く。シート4枚、3桁区切り、見出し太字、列幅調整。openpyxl依存はここだけに閉じ込める。
  ／なぜ分けるか：見た目の要求は一番よく変わる（色・順番・フォント）。集計ロジックと同居させると、見た目いじりで集計を壊す。openpyxl で悩むのもこのファイル内に限定できる。

- errors.py: 自作の例外型を1つ定義（ファイルパス・行番号・原因メッセージを持つ）。文字列化すると「data/sales_202607_nagoya.csv の5行目：金額 'xxx' を数値に変換できません」を返す。
  ／なぜ分けるか：SPECの「どのファイルの何行目か表示して止まる」を実現する中心。型が1つあれば main で1箇所 catch すれば済み、想定外のバグはそのまま落ちて調査しやすい。

- main.py: loader → aggregate → excel を順に呼ぶだけ。引数なし（python main.py のみ）。errors.py の例外型を1箇所で受け止め、メッセージを出して終了コード1で止まる。
  ／なぜ分けるか：進行役に徹する。計算やI/Oの詳細を持たせないことで、全体の流れが1画面で読める。

### 設計の芯

先に15ファイルを1枚の縦持ち表に正規化してから集計する。
欲しい集計は4種類あり、それぞれまとめ方（軸）が違う。ファイルを1個読むたびにその場で集計すると、15ファイル×4種類の中間結果を別々に抱えて、あとで店舗・月ごとに突き合わせて足すことになる。これは発注者がいま手作業でやっているExcelコピペと同じ苦しみで、突き合わせのたびにミスの余地が生まれる。
先に「全店・全月の明細が入った1枚のきれいな表」を作れば、集計はその1枚に「店舗でまとめて」「カテゴリでまとめて」と指示するだけ。あとから「曜日別も」と言われても同じ表に足すだけで済む。汚れの吸収（読み込み）と集計を、この1枚の表を境目にはっきり分けられる。

### 決めたこと

- 行番号の追跡方法：1ファイルを表にした時点で「ファイル名」列と「元のCSV行番号」列を付ける。CSV行番号 = pandasのindex + 2（ヘッダ1行ぶんで +1、0始まりを1始まりにして +1）。15ファイルを連結するとindexが重複してあとから計算できなくなるので、連結前に確定させる。実際に +2 で合うかは、わざと壊したファイルで1回確認して合わせ込む。
aggregate.py は、この2列があることを知っているべきか? 知らないべきか?→知っていていい．無視するだけ

- 売上の正：金額列（売上金額／金額）をそのまま合計する。単価×数量では再計算しない。SPECの集計は全て「売上金額」ベースで、実務では割引で単価×数量と一致しないこともあるため、CSVに書かれた金額を正とする。

- 店舗名：config.py に slug→日本語のマップを持つ（shinjuku→新宿店 など）。未知slugが来たら errors.py の例外で止める（会議資料に shinjuku のような生のslugが載るのを防ぐ。SPECの「おかしければ止まる」方針とも揃える）。

- 年月の表記：「2026-06」の文字列。ゼロ埋めしてあるので、文字列のまま並べても辞書順＝時系列順になる。Excelの見出しにもそのまま使える。

### 今回やらないこと（追記）

- 1行の不正で止めず、その行だけ飛ばして続けるモード（strict切り替え）。今回はSPEC通り「最初の不正で止まる」。将来の追加要望として想定だけしておく。
- グラフの自動生成
- 前月比・前年比などの分析