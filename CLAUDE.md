# 店舗別売上集計ツール（ポートフォリオ(1)）

## このプロジェクトの目的

副業受託のポートフォリオ。「複数のCSVを、決まったルールで集計して、決まった形式のExcelで出力する」という型を、自分の手で書けるようにするための練習台。
成果物そのものより、**中身を全部説明できる状態で完成させること**が目的。

## 私（開発者）について

- Python は大学の授業と研究で使用。研究では既存のPyTorchコードを読んで自分のデータで動くよう改変した経験がある
- **ゼロから設計して書いた経験は少ない。そこを埋めるのがこのプロジェクト**
- pandas / openpyxl は初めて

## Claude への依頼ルール（重要・このプロジェクトの間は厳守）

このプロジェクトでは、**あなた（Claude）は実装コードを書かないでください。**

### やってほしいこと

- 設計の相談に乗る（どう分割すべきか、どこでエラーが起きうるか）
- 私が書いたコードのレビュー（問題点の指摘。**修正版は出さない**）
- エラーの原因を「説明」する（**修正コードは出さない**）
- ライブラリの使い方や概念の説明
- 「なぜこの書き方なのか」の解説

### やらないでほしいこと

- 私が書く前に実装コードを提示すること
- 「直しておきました」と勝手に修正すること
- 私が理解していないまま先に進めること

### 私が「実装して」と明示的に言った場合

そのときだけ書いてください。ただし書いた後に、**各ブロックが何をしているかを1行ずつ説明**してください。私がそれを読んで理解できるかが受け入れ条件です。

IMPORTANT: 私が「書いて」と言っていないのに実装コードを出力しない。これはこのプロジェクトで最も重要なルールです。

## 開発環境

- Windows
- Python 3.x
- 使用予定: pandas, openpyxl

## コマンド

```
python setup_data.py     # サンプルCSVを生成（最初に1回）
python main.py           # data/ を読んで output/ に集計結果を出す
python -m pytest         # テスト（作ったら）
```

## ディレクトリ構成

```
.
├── data/          # 入力CSV（店舗別・月別の売上データ、15ファイル）
├── output/        # 出力先（集計済みExcel）
├── main.py        # エントリポイント
├── setup_data.py  # サンプルデータ生成
├── SPEC.md        # 仕様
└── CLAUDE.md      # このファイル
```

## 入力データの注意点（実務を模して意図的に汚してある）

- ファイルによってヘッダ名が違う（`売上金額` と `金額`）
- 日付形式が混在（`2026-06-01` と `2026/7/1`）
- 金額にカンマが入っている行がある（`"2,040"` → 文字列として読まれる）
- BOM付きUTF-8
- 一部ファイルの末尾に空行がある

**これらを吸収するのがこのツールの価値。** 素直に読むだけでは必ず落ちる。